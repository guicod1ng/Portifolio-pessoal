# TODO: Language Toggle Fix

## Steps

- [x] Fix `assets/lang.js` — fix `window.i18n.idiomaAtual` bug, add dynamic mode labels, add missing translation keys
- [x] Fix `script.js` — update `aplicarTextos()` to support placeholders, `<html lang>`, and `<title>`; fix `setTheme()` dynamic labels
- [x] Fix `index.html` — add `data-i18n` to missing elements, resolve duplicate `sobreTitulo` key conflict
- [x] Verify language toggle works by clicking the button
